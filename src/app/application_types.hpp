#pragma once

namespace hammock::app {
    /// @enum ExecutionMode
    /// @brief Defines how the runner launches the engine
    enum class ExecutionMode {
        Engine,
        Game,
    };
}  // namespace hammock::app